# Changelog

## 0.1.0

- Erster Release des ESPHome Firmware Relay
- Lokaler Firmware-Cache mit Sync-Delete
- GitHub Release API Integration (PAT-basiert)
- Webhook-Support (HMAC-SHA256) für automatische Sync-Trigger
- Ingress-Dashboard mit Geräte-Status (lokal/remote Versionen)
- Endpoints: `/health`, `/status`, `/devices`, `/refresh`, `/webhook/github`
- OTA-Manifest-Rewrite für lokale Firmware-URLs
